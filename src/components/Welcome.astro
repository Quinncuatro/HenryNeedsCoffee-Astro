---
// Calculate the terminal-style current login date
const now = new Date();
const days = ["Sun","Mon","Tue","Wed","Thu","Fri","Sat"];
const months = ["Jan","Feb","Mar","Apr","May","Jun","Jul","Aug","Sep","Oct","Nov","Dec"];

const zerosAreHard = (n) => {
  return n < 10 ? "0" + n : n;
};

const day = days[now.getDay()];
const month = months[now.getMonth()];
const date = now.getDate();
const hours = zerosAreHard(now.getHours());
const minutes = zerosAreHard(now.getMinutes());
const seconds = zerosAreHard(now.getSeconds());
const year = now.getFullYear();

const currentLoginPrompt = `Current login: ${day} ${month} ${date} ${hours}:${minutes}:${seconds} ${year}`;

const featuredProjects = [
  {
    title: "Project 1",
    description: "Description of Project 1",
    link: "https://github.com/quinncuatro/project1"
  },
  {
    title: "Project 2",
    description: "Description of Project 2",
    link: "https://github.com/quinncuatro/project2"
  },
  {
    title: "Project 3",
    description: "Description of Project 3",
    link: "https://github.com/quinncuatro/project3"
  }
];
---

<div class="terminal-window">
  <div class="terminal-content">
    <span class="login-prompt">{currentLoginPrompt}<br />
    Welcome to HenryNeeds.Coffee!</span>
    <br /><br />
    <nav class="menu-bar">
      <a href="/" class="active">Home</a>
      <a href="/blog">Blog</a>
      <a href="/digital-garden">Digital Garden</a>
      <a href="/resume">Resume</a>
      <a href="/talks-and-pods">Talks & Pods</a>
      <a href="/contact">Contact</a>
    </nav>
    <p class="command-prompt">[Guest@HenryNeeds ~]$ <span class="typed">whoami<span class="cursor"></span></span></p>

    <div class="intro-section">
      <h1>Hi, my name is Henry!</h1>
      <div class="profile-section">
        <div class="profile-text">
          <p>I'm a Senior DevOps Engineer at <a href="https://ted.com/">TED</a>, maintain a <a href="https://techenthusiastscholarship.com/">Tech Enthusiast Scholarship</a>, created <a href="https://newhavenreliefresources.com/">New Haven Relief Resources</a>, and occasionally award devs with <a href="https://henrygives.coffee/">Henry Gives Coffee</a>.</p>
          <p>Some friends and I run <a href="https://newhaven.io/">NewHaven.IO</a> and if you're somewhat local, I hope you'll come join our <a href="https://discord.gg/gM3zkw9MvP">Discord server</a>.</p>
          <p>I made an eLearning course called <a href="https://www.udemy.com/course/introduction-to-server-administration/">Intro to Server Administration</a>, and love to help people learn.</p>
          <p>Please <a href="https://henryneeds.coffee/contact">hit me up</a> if you want to collab on anything!</p>
        </div>
      </div>
    </div>

    <div class="featured-section">
      <h2>Featured Projects</h2>
      <div class="project-grid">
        {featuredProjects.map(project => (
          <div class="project-card">
            <h3>{project.title}</h3>
            <p>{project.description}</p>
            <a href={project.link} target="_blank" rel="noopener noreferrer" class="project-link">View Project</a>
          </div>
        ))}
      </div>
    </div>

    <div class="recent-posts-teaser">
      <h2>Recent Writing</h2>
      <p>Check out my <a href="/blog">blog</a> for articles on tech, DevOps, and web development.</p>
      <p>Or explore my <a href="/digital-garden">Digital Garden</a> for works-in-progress and growing ideas.</p>
    </div>

    <div class="contact-teaser">
      <h2>Get In Touch</h2>
      <p>Interested in collaborating or just want to say hi? Visit my <a href="/contact">contact page</a> to reach out!</p>
    </div>
  </div>
</div>

<style>
  .terminal-window {
    max-width: 900px;
    margin: 20px auto;
    background-color: var(--bg-color);
		border-radius: 8px;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
    overflow: hidden;
	}

  .terminal-content {
    padding: 2rem;
    color: var(--text-color);
  }

  .login-prompt {
    color: #fff;
    font-family: monospace;
    white-space: pre-wrap;
  }

  .menu-bar {
			display: flex;
    gap: 1rem;
    margin-bottom: 1.5rem;
			flex-wrap: wrap;
		}

  .menu-bar a {
    color: var(--accent-color);
    text-decoration: none;
    transition: color 0.2s;
    font-weight: 500;
		}

  .menu-bar a:hover {
    text-decoration: underline;
  }

  .menu-bar a.active {
    color: var(--accent-color-dark);
    font-weight: bold;
  }

  .command-prompt {
    font-family: monospace;
    margin-bottom: 2rem;
  }

  .typed {
    color: var(--text-color);
  }

  .cursor {
    display: inline-block;
    width: 0.5em;
    height: 1em;
    background-color: var(--accent-color);
    animation: blink 1s step-end infinite;
    vertical-align: text-bottom;
    margin-left: 2px;
  }

  @keyframes blink {
    from, to { opacity: 1; }
    50% { opacity: 0; }
  }

  .intro-section {
    margin-bottom: 3rem;
  }

  .intro-section h1 {
    color: var(--accent-color);
    margin-bottom: 1.5rem;
    font-size: 2.5rem;
  }

  .profile-section {
    display: flex;
    gap: 2rem;
    align-items: center;
  }

  .profile-text p {
    margin-bottom: 1rem;
    line-height: 1.6;
  }

  .featured-section,
  .recent-posts-teaser,
  .contact-teaser {
    margin-bottom: 3rem;
  }

  h2 {
    color: var(--accent-color);
    margin-bottom: 1.5rem;
    font-size: 1.8rem;
  }

  .project-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
    gap: 1.5rem;
    margin-bottom: 2rem;
  }

  .project-card {
    padding: 1.5rem;
    border: 1px solid #eee;
    border-radius: 8px;
    background-color: var(--card-bg);
    transition: transform 0.3s, box-shadow 0.3s;
  }

  .project-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
  }

  .project-card h3 {
    color: var(--accent-color);
    margin-top: 0;
    margin-bottom: 1rem;
  }

  .project-link {
    display: inline-block;
    margin-top: 1rem;
    padding: 0.5rem 1rem;
    background-color: var(--accent-color);
    color: white;
    border-radius: 4px;
    text-decoration: none;
    font-size: 0.9rem;
    transition: background-color 0.3s;
  }

  .project-link:hover {
    background-color: var(--accent-color-dark);
    text-decoration: none;
  }

  .recent-posts-teaser,
  .contact-teaser {
    background-color: var(--card-bg);
    padding: 1.5rem;
    border-radius: 8px;
    border: 1px solid #eee;
  }

  .recent-posts-teaser p,
  .contact-teaser p {
    margin-bottom: 0.5rem;
  }

  @media (max-width: 768px) {
    .terminal-content {
      padding: 1.5rem;
    }

    .intro-section h1 {
      font-size: 2rem;
    }

    .profile-section {
      flex-direction: column;
      text-align: center;
    }

    .project-grid {
      grid-template-columns: 1fr;
    }

    .menu-bar {
      justify-content: center;
    }
  }
</style>
